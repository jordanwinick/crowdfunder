<%= form_for @project do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.label :goal %>
  <%= f.number_field :goal %>
  <%= f.label :start_date %>
  <%= f.date_field :start_date %>
  <%= f.label :end_date %>
  <%= f.date_field :end_date %>
  <div id="rewards">
    <%= f.fields_for :rewards do |reward| %>
      <%= render 'reward/form', :f => reward %>
    <% end %>
    <div class="links">
      <%= link_to_add_association 'add reward', f, :rewards %>
    </div>
  </div>
  <%= f.button "Create Project" %>
<% end %>